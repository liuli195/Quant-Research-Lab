## 1. 同步契约修复

- [x] 1.1 以 TDD（测试驱动开发）修复历史回测补录导致 `latest_backtest_id`（最新回测编号）回退的问题，并运行定向测试
- [ ] 1.2 以 TDD 将官方摘要唯一输出路径改为 `data/official-summary.csv`，增加版本化来源、编码、表头、行数和关联数据集证据，并拒绝旧路径

## 2. 归档说明

- [ ] 2.1 更新清单与操作文档，说明官方摘要字段关系、`reports/` 目录边界、兼容迁移和当日盈亏的数据选择

## 3. 数据迁移与历史对象补录

- [ ] 3.1 移动全部既有官方摘要到 `data/`，更新并验证清单路径与证据，确认旧文件和旧引用均为零
- [ ] 3.2 通过发布入口逐项同步并验证策略 001 的页面序号 `1、9、75、76、83、87、88`，为完整或部分对象保存权威清单和证据

## 4. 完整验收

- [ ] 4.1 验证本地目录覆盖 1–115、最新回测编号仍为 115，运行相关测试、完整构建验证和发布入口端到端回归
