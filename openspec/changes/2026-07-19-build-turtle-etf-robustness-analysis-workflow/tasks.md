## 1. 锁定标准来源与能力契约

- [x] 1.1 先编写失败的来源登记契约测试，再定义版本化清单，绑定场景、仓库相对路径、预期清单摘要、来源类型与聚宽模拟快照身份，拒绝 `latest`（最新）、扫描和越界路径
- [x] 1.2 先编写三类来源、摘要不符和模拟快照路径不符的失败测试，再实现只读验证与来源能力摘要

## 2. TDD 接入已对齐的共同事实表

- [x] 2.1 先编写聚宽模拟交易根清单加指定快照的失败测试，再以最小改动扩展 `analysis_data`（分析数据）接纳模拟交易，不复制或改写归档
- [x] 2.2 先编写本地研究、聚宽回测和聚宽模拟交易四张共同事实表的一致性测试，再复用现有视图计算共同绩效、风险与独立基准
- [x] 2.3 先编写本地缺少物理风险表、模拟交易官方风险额外字段和数据集缺失测试，再实现来源参考与“证据不足”状态，不伪造共同字段

## 3. TDD 接入能力降级的深度归因与稳健性

- [x] 3.1 先编写本地归因扩展、聚宽归因数据集、摘要冲突和归因缺失测试，再投影为同一只读归因输入并输出能力状态
- [x] 3.2 先编写单来源和多场景的区块抽样、历史压力、持仓冲击、CVaR（条件风险价值）与证据不足测试，再复用并收紧现有确定性稳健性计算
- [x] 3.3 先编写通过、不通过、证据不足和已知失败并存测试，再完成分项证据矩阵与不伪造的聚合规则

## 4. 完成标准分析入口与交付

- [x] 4.1 先编写入口参数与停止状态的失败测试，再实现由 Skill（技能）调用的标准分析入口，按来源验证、共同分析、可用归因、稳健性和报告顺序编排
- [x] 4.2 生成绑定来源能力、清单摘要、模拟快照、配置、脚本版本和输出摘要的确定性 JSON（结构化数据）与 Markdown（标记文档）交付，并验证重复运行不修改原始结果

## 5. 发布入口与完整回归验证

- [x] 5.1 先编写失败的 Skill（技能）布局测试，再创建唯一 `.agents/skills/analyze-quant-robustness/`、自包含 `scripts/quant_analysis/`（量化分析）计算包、界面元数据和 `.claude/skills/` 相对符号链接，并使测试通过；不保留旧入口兼容层
- [x] 5.2 使用项目 `.venv` 运行来源契约、共同表、模拟快照、归因、稳健性和报告的定向测试
- [x] 5.3 从真实 Skill（技能）发布入口运行临时目录 E2E（端到端）回归，覆盖三类来源、归因缺失、稳健性证据不足和多场景通过，验证不访问网络、不触发上游流程且原始摘要不变
- [x] 5.4 已于 2026-07-19 取得完整模式确认，并通过项目 `.venv` 完成构建检查和 `verify --full`（完整验证）：构建状态 `passed`；19 项完整检查全部通过，`full-not-run: false`、最终状态 `passed`，墙钟时间 45.0 秒；另有迁移、交付与双向解耦定向回归 265 项通过；Standard Review（标准审查）为 0 个 Critical（致命）、0 个 Important（重要）、0 个 Minor（次要），Ponytail Review（马尾审查）无可删项
