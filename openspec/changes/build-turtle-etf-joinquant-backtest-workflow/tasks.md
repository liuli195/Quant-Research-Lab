## 1. 锁定外部身份与正式口径

- [ ] 1.1 确认并记录新海龟 ETF（交易型开放式指数基金）的本地 `strategy_id`、聚宽策略名称和详情页身份，验证三者唯一对应
- [ ] 1.2 确认并记录正式基线起止日期、数据截止日和滚动窗口最后可用季度，生成不可歧义的日期配置
- [ ] 1.3 确认沪深300与纳斯达克100人民币 Total Return（总回报）的来源、分红再投资、汇率转换和版本证据，验证能覆盖正式区间
- [ ] 1.4 确认聚宽提交与恢复使用的交互入口及人工确认点，记录允许动作和停止状态且不保存任何凭证

## 2. 以 TDD 建立通用回测编排能力

- [ ] 2.1 先写失败的 layout（布局）与解耦测试，再创建唯一 `.agents/skills/run-joinquant-backtests` Skill（技能）、`.claude` 相对符号链接、`agents/openai.yaml` 和最小 CLI（命令行接口），使测试通过
- [ ] 2.2 先写场景清单合法与非法用例，再实现 schema（结构约束）和校验器，覆盖必需字段、空目标、`latest`、`all`、裸远端 ID 及敏感字段拒绝
- [ ] 2.3 先写摘要、重复和同名漂移用例，再实现规范化与 SHA-256（文件摘要）场景指纹，验证相同输入稳定且漂移不会覆盖旧结果
- [ ] 2.4 先写阶段顺序和场景顺序用例，再实现固定阶段、清单顺序校验与确定性执行计划
- [ ] 2.5 先写中断恢复用例，再实现追加式运行台账和状态转换，验证已完成场景被跳过、未完成场景可继续
- [ ] 2.6 先写明确目标与部分归档用例，再实现调用 `joinquant-archive-sync`（聚宽归档同步）公开入口的交接和覆盖率门禁，不导入或复制其内部实现
- [ ] 2.7 完成 `SKILL.md` 编排说明、停止状态和 CLI 参考，验证文档不含海龟规则、固定策略身份、固定场景或凭证

## 3. 建立海龟正式回测项目清单

- [ ] 3.1 用已锁定身份建立功能校验场景，固定本地代码摘要并声明 API（接口）、调度、订单、费用和次日开盘执行检查项
- [ ] 3.2 建立人民币 1,500,000 元、统一区间和统一适用费用的海龟、`strategy-001`、`strategy-002` 三策略正式基线场景
- [ ] 3.3 建立两个人民币总回报指数比较配置，验证来源、分红、汇率、日期与三策略基线一致且不拼接旧结果
- [ ] 3.4 生成 40/60 日入场、1.5N/2.5N 止损、120 日/30 日半衰期 EWMA（指数加权移动平均）协方差六个单项参数场景并核对继承摘要
- [ ] 3.5 生成三个固定历史时期和按季度移动的完整三年滚动窗口场景，验证每个场景使用独立初始账户
- [ ] 3.6 生成 11 个逐 ETF 删除和 6 个逐资产组删除场景，验证恰好 17 个且其余设置与基线一致
- [ ] 3.7 生成五个佣金、滑点和延迟执行场景，验证延迟场景仅推迟原订单且不读取延迟日新信息
- [ ] 3.8 展开并冻结全部预期场景集合，运行通用预检确认摘要唯一、阶段和场景顺序明确、没有隐式目标或敏感信息

## 4. 完成聚宽功能校验与正式基线

- [ ] 4.1 在 JoinQuant（聚宽）运行功能校验并核对远端代码摘要、调度、订单、费用和执行时点，失败时停止后续批次
- [ ] 4.2 使用明确详情链接归档功能校验结果并通过既有 manifest（清单）门禁，更新台账中的正式远端身份
- [ ] 4.3 在聚宽运行海龟正式基线，核对人民币 1,500,000 元、锁定区间、佣金、滑点和代码摘要
- [ ] 4.4 在聚宽以同资金、同区间和适用费用分别重跑 `strategy-001` 与 `strategy-002`，不修改其策略规则
- [ ] 4.5 取得并归档两个指数总回报比较序列，核对日期、分红再投资和纳斯达克100人民币汇率转换证据

## 5. 完成所有改变交易路径的聚宽场景

- [ ] 5.1 在聚宽运行六个参数邻域场景，逐项核对只改变目标参数并记录明确回测详情链接
- [ ] 5.2 在聚宽运行三个固定时期场景，逐项核对独立初始账户、实际可用交易日和详情链接
- [ ] 5.3 按确定性批次运行或恢复全部三年滚动窗口场景，直到清单中每个窗口均有唯一详情链接或明确失败状态
- [ ] 5.4 在聚宽运行 11 个逐 ETF 删除和 6 个逐资产组删除场景，核对资产差异与冻结清单一致
- [ ] 5.5 在聚宽运行五个佣金、滑点和延迟执行场景，核对费用摘要及原信号延迟语义

## 6. 完成明确目标归档与覆盖交付

- [ ] 6.1 按每个场景的明确详情链接同步基线、参数、时期、滚动、资产删除和成本执行结果，拒绝任何 `latest` 或猜测目标
- [ ] 6.2 对每个正式场景运行既有归档完整性门禁，核对代码、参数、订单、持仓、净值、费用、日志和报告证据，保留部分或失败状态
- [ ] 6.3 生成场景—归档映射和覆盖报告，验证预期集合与通过门禁的实际集合完全一致后才交付稳健性分析流程

## 7. 发布验证与完整回归

- [ ] 7.1 使用 Skill Creator（技能创建器）的 `quick_validate.py` 校验通用 Skill，并运行仓库 layout 测试验证唯一真实目录、符号链接和发布文件
- [ ] 7.2 使用项目 `.venv` 运行清单、摘要、去重、顺序、恢复、明确目标和覆盖率全部单元测试并修复失败
- [ ] 7.3 从 `.agents` 真实 Skill 发布入口运行一次不访问网络的正式 E2E（端到端）自检，覆盖摘要、去重、顺序、恢复和覆盖率
- [ ] 7.4 从正式 Skill 入口回归“预检→聚宽功能校验→正式场景→明确目标归档→覆盖交付”主流程，并运行仓库完整验证与严格 OpenSpec（开放规格）校验
