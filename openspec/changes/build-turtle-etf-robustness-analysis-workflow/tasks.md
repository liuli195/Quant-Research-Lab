## 1. 锁定分析契约

- [ ] 1.1 在查看正式分析结果前锁定 CAGR（复合年增长率）年化口径、连续 5 日 CVaR（条件风险价值）窗口、历史压力恢复时间定义和固定随机种子，并用边界样例验证配置可解析且摘要稳定
- [ ] 1.2 定义并验证正式结果清单、分析配置、运行清单和三态证据矩阵的版本化格式，覆盖显式运行身份、文件摘要、场景关系、允许变化字段和项目专属门槛

## 2. TDD 初始化通用 Skill 与基础分析

- [ ] 2.1 先编写失败的 Skill（技能）布局测试，再用 `init_skill.py` 初始化 `.agents/skills/analyze-quant-robustness/`、界面元数据和 `.claude/skills/` 相对符号链接，并使唯一实现测试通过
- [ ] 2.2 编写精简 `SKILL.md` 和按需引用资料，只保留前置检查、脚本调用、停止状态及结果交付，并验证其中不含海龟资产、参数或门槛
- [ ] 2.3 先编写正式证据完整、缺失、摘要冲突和隐式 `latest`（最新）选择的失败测试，再实现只读输入门禁并使缺失证据稳定输出“证据不足”
- [ ] 2.4 先编写 CAGR、最大回撤和 Calmar（卡玛比率）的黄金样例与边界测试，再实现确定性通用指标计算并记录公式、输入和脚本摘要

## 3. TDD 实现风险与尾部分析脚本

- [ ] 3.1 先编写固定种子、极端亏损和重复执行测试，再实现配置化 Block Bootstrap（区块自助抽样）的连续区块、确定性批处理及结果摘要
- [ ] 3.2 先编写窗口边界、最低点和恢复状态测试，再实现配置化历史压力窗口计算及无法成交与风险记录关联
- [ ] 3.3 先编写资产分类、持仓来源和缺失风险字段测试，再实现只对正式收盘后持仓静态重估的配置化假设冲击
- [ ] 3.4 先编写并列分位点、极端收益和连续复利测试，再实现单日及多日非参数 Historical CVaR（历史条件风险价值）
- [ ] 3.5 先编写通过、不通过、证据不足及已知失败与缺证据并存测试，再实现不可补偿的逐项证据矩阵和三态整体聚合

## 4. 完成通用编排入口

- [ ] 4.1 实现由 Skill（技能）调用的统一分析入口，按输入门禁、指标、抽样、压力、冲击、CVaR 和证据矩阵顺序编排，并用失败注入测试验证停止状态
- [ ] 4.2 生成包含输入、配置、随机种子、脚本版本和输出摘要的只读运行清单及报告，验证重复运行不覆盖正式归档且相同输入产生相同结果

## 5. 配置海龟 ETF 评估实例

- [ ] 5.1 创建并校验海龟项目专属的基线、六个参数变体、三个固定时期、季度三年窗口、11 个逐 ETF 删除、6 个逐资产组删除和 5 个成本执行场景配置
- [ ] 5.2 创建并校验 5、20、60 日区块各 10,000 条 756 交易日路径、五个历史窗口、四个假设冲击、三项 CVaR 及全部固定硬门槛配置
- [ ] 5.3 在前置聚宽回测流程完成后建立显式正式快照清单，逐项绑定回测身份、来源链接、代码参数摘要、所需数据集和文件 SHA256（完整性摘要），不得使用最新结果选择器

## 6. 执行正式证据分析并形成结论

- [ ] 6.1 对海龟正式快照运行输入与跨场景口径门禁，证据完整时锁定本次输入，证据缺失时生成列明补跑对象的“证据不足”结果并停止后续裁决
- [ ] 6.2 对完整正式证据计算并复核基线、参数、时期、资产和成本执行全部门槛，验证较差场景未被删除且任何指标不能补偿失败
- [ ] 6.3 执行三组区块抽样、五个历史窗口、四个持仓冲击和三项 CVaR，复核固定路径数、持仓来源、尾部样本及每项硬门槛
- [ ] 6.4 汇总唯一的“通过”“不通过”或“证据不足”结论和完整证据矩阵，并验证流程未修改策略、正式归档、冻结状态或前向模拟状态

## 7. 发布入口与完整回归验证

- [ ] 7.1 使用 Skill Creator（技能创建指南）的 `quick_validate.py` 和仓库布局检查验证触发描述、目录结构、唯一真实实现及 Codex/Claude（代码代理）兼容入口
- [ ] 7.2 通过项目 `.venv` 运行全部单元、黄金样例、输入契约和确定性重复测试，并保存可复核的通过记录
- [ ] 7.3 从真实 Skill（技能）发布入口运行临时目录 E2E（端到端）回归，覆盖全部分析器及通过、不通过、证据不足三条完整业务路径，验证不访问网络或历史业务归档
- [ ] 7.4 在执行前取得 Build and Verify（构建与验证）完整模式所需确认，再通过项目 `.venv` 运行构建检查和 `verify --full`（完整验证），保存完整回归结果并逐项复核需求覆盖
