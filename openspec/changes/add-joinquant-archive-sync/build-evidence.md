# Build evidence（实施证据）

## 已验证

- 仓库 full（完整）验证：192 项通过；OpenSpec strict（严格规格）通过。Windows 上 `-n auto` 的 20 个并行进程曾触发浏览器进程与内存资源耗尽；本次以同一 full 入口设置 xdist（并行测试）自动并发为 4，192 项全部通过。覆盖 1000 条免费窗口滚动后的原始页累计、Research 来源链、`capped_free` 续传、增量日志、逐版本真实代码及源 ID 映射、生命周期单一归因所有权、回测最终资产关联、性能面板全状态语义复核、发布编排入口，以及畸形响应先保存原文和恢复结果再报告失败。
- Codex（代码代理）与 Claude（代码代理）从各自仓库 Skill（技能）发布入口启动项目 `.venv` 子进程；均完成生产编排 `sync_all_active_simulations`、首次 `committed`、第二次 `unchanged`、DuckDB 内存查询和 CSV 导出，约 1.7 秒、峰值约 26.7 MB。
- 真实历史回测 115 的 PoC 已取得 1289 行结果、1289 行资金、2386 行持仓、378 行订单、1000 条完整普通日志、1631 条可查询归因日志和 136,738 字节性能分析文本；这些数据证明外部链路可行。
- 真实活动模拟交易 `ETF动态调仓` 的 PoC 已取得连续 1000 条免费日志窗口并确认源代码未实现归因写入器，因此归因应为 `missing_at_source`。
- `etf_factor_rotation` 活动模拟交易共有 11 条代码历史、7 份不同代码、5 个不同归因路径。清单保存全部 11 条“历史顺序/时间、`sourceBacktestId` → 代码 SHA256/文件”映射，但只读取启动生命周期实际初始化的 `default-full-run` 路径；最终归档只含该模拟交易的 30 条记录，另外 4 个历史回测文件及其 4913 行已从模拟交易快照删除。第二次真实同步两个活动对象均返回 `unchanged`。
- thorough（彻底）审查和用户复核发现原多源设计把“代码历史出现路径”误当成“模拟交易拥有路径”。实现现已删除多源读取与落盘分支，强制模拟交易代码历史映射存在，并增加回测 `run_end.total_value`、`cash` 与 Research 最终资金记录的离线关联门禁。
- Windows 临时计划任务已真实创建、执行同一 `self-test`、读取退出码并清理；正式 `JoinQuantArchiveSync` 已安装为北京时间每天 04:00，失败后每 30 分钟最多重试 3 次，命令入口为仓库内 Skill 脚本。
- Git LFS（大文件存储）属性覆盖 gzip、Parquet 和 CSV；远端 LFS locks 接口可用。当前分支已上传 45 个 LFS 对象（974 KB）；随后从远端分支创建隔离 detached checkout（分离检出）、执行 `git lfs pull`，回测 115 和 `ETF动态调仓` 均从恢复文件通过严格校验，证明新归档可远端恢复。
- 已验证认证成功后生成 2266 字节 DPAPI 密文，并由全新无界面进程恢复登录、列出策略 `etf_factor_rotation` 的 115 个回测候选；未消费积分，未保存或提交账号、密码、明文 Cookie、Token 或浏览器配置。

## 外部受限 / 待验证

- 已用生产入口严格重同步回测 115：1289 行结果、1289 行资金、2386 行持仓、378 行订单、10 行分期风险、1000 条完整普通日志、1631 条完整归因日志和 1756 行性能分析均通过清单与文件摘要校验，门禁无例外。清单引用的原始证据为 5,208,355 字节，落盘引用文件为 831,073 字节，约压缩 6.267 倍。
- `ETF动态调仓` 已完成新版清单迁移并通过严格门禁；页面未提供性能面板，故以页面能力证据标记 `unsupported_api_version`，启动代码没有归因写入器，故归因标记 `missing_at_source`。`etf_factor_rotation` 已通过启动生命周期单一路径归因同步与严格校验。正式 04:00 任务已安装并由 `schedule-status` 确认配置与仓库入口一致。
