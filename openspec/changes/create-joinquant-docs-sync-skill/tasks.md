## 1. Skill 与入口

- [x] 1.1 使用 Skill Creator（技能创建器）初始化 `joinquant-docs-sync`，补齐中文 `SKILL.md`、界面元数据、官方来源清单和 Claude（代码代理）符号链接入口

## 2. 同步核心

- [x] 2.1 以 TDD（测试驱动开发）实现统一 CLI（命令行接口）的 `preview`、`sync`、`verify`、`self-test`，覆盖渲染转换、API（接口）索引、幂等写入和失败保留

## 3. 仓库验证

- [x] 3.1 扩展 Skill（技能）布局与 Build and Verify（构建与验证）配置，运行公开入口端到端回归、Skill（技能）校验和仓库完整验证
