## 1. 测试执行兼容性

- [x] 1.1 让直接访问 `.py_func` 的既有测试在关闭 Numba JIT 时执行同一 Python 函数，并保留真实 JIT 专项入口
- [x] 1.2 分别测量本地量化单元测试、E2E 和真实 JIT 最小用例，确定满足覆盖要求的最小并行分组

## 2. 验证编排

- [x] 2.1 优化等价外部写入状态扫描、连续价格派生和现金二分重复计算，并调整 `.build-and-verify/config.json`，关闭非专项测试的 JIT、按瓶颈拆分入口并消除无效嵌套并行
- [x] 2.2 同步更新配置契约测试，证明全部测试、完整 E2E、真实 JIT 和 OpenSpec 严格校验仍在全量入口中

## 3. 严格验收

- [x] 3.1 运行全部受影响测试和 OpenSpec 严格校验，确认无联网调用、无仓库外部写入和无测试缺失
- [x] 3.2 连续执行非缓存 `verify --full`，确认每次成功且总耗时不超过 30 秒
