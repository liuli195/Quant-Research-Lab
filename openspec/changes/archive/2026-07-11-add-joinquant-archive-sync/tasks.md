本 change（变更）保持单一实施序列：页面目录、同步、日志、存储、查询和仓库 Skill（技能）共用同一清单与完整性门禁，任何部分都不能独立完成端到端验收。

## 1. 真实下载 PoC 与核心契约

- [x] 1.1 用项目 `.venv` 和现有 Playwright（浏览器自动化）实现最小认证命令，使用仓库外专用浏览器目录并验证登录失效返回 `auth_required`
- [x] 1.2 对一个存在归因写入器的已完成回测执行“已登录页面 → Research API（研究接口）与聚宽官方导出 → 捕获下载 → 本地临时文件”的真实 PoC（可行性原型），记录文件大小、摘要、归因日志完整性和可重复步骤
- [x] 1.3 为自动下载失败实现显式本地文件导入兜底，并用同一校验入口证明兜底文件等价；PoC 两条路径都失败时停止后续实施
- [x] 1.4 定义页面对象目录、稳定键/远端别名、数据集清单和五种完整性状态的最小 JSON Schema（数据约束），增加一个可运行契约测试

## 2. 单一同步 CLI 与目标解析

- [x] 2.1 建立一个 Python CLI（命令行接口），提供 `auth`、`list-targets`、`sync-backtest`、`sync-active-simulations`、`verify`、`query`、`export-csv` 和 `self-test` 入口
- [x] 2.2 实现策略列表和页面路径读取，将远端传输标识保存为别名，并验证刷新后标识变化不会创建重复目录
- [x] 2.3 实现历史目标校验：只接受明确页面序号或详情链接，拒绝缺失目标、`latest`（最新）和隐式全量；多个目标必须逐项列出
- [x] 2.4 实现对象级文件锁、隔离临时目录和校验后原子提交，验证失败批次不会覆盖已有完整归档

## 3. 代码、结构化数据与完整性门禁

- [x] 3.1 同步策略默认代码、回测完整代码和模拟交易代码历史，并为每份代码记录 SHA256（完整性摘要）；不为聚宽不存在的独立构建对象杜撰目录或入口
- [x] 3.2 通过 Research API（研究接口）分页同步结果、资金、持仓、订单、自定义记录、风险和分期风险，覆盖已完成、失败和取消状态语义
- [x] 3.3 实现字段、行数、唯一键、排序、时间范围、交易日关联和明确分页终止校验，任何预期数据集缺少状态时拒绝对象通过门禁
- [x] 3.4 实现同步前后远端清单围栏、摘要和游标增量判定，验证重复同步只补缺失或变化部分
- [x] 3.5 生成对象报告和清单摘要，明确列出 `complete`、`capped_free`、`missing_at_source`、`unsupported_api_version` 和 `failed` 数据集

## 4. 日志同步与费用边界

- [x] 4.1 实现先保存原始响应再解析的日志管道，对不合法 JSON（数据格式）执行容错恢复并记录错误位置和恢复数量
- [x] 4.2 把归因 JSONL（逐行数据）作为独立核心数据集，校验逐行解析、Token（标识）、序列、记录数、摘要、`run_start` 和已结束运行的 `run_end`；任何不完整都阻断门禁
- [x] 4.3 实现失败运行错误日志完整同步；普通控制台日志/性能响应必须取得全部免费内容，在 1000 条边界探测下一页后才判定 `complete` 或 `capped_free`
- [x] 4.4 根据代码证据识别未实现归因写入的运行并记录 `missing_at_source`，不得生成伪造日志
- [x] 4.5 实现按运行与日志类型指定的积分导出预览；只有调用者确认聚宽返回的完整日志计费范围、积分价格和本地保留范围后才能下载一次完整日志并在本地保留指定部分

## 5. 紧凑存储、查询与 Vibe-Trading 输出

- [x] 5.1 将原始 JSON/JSONL 证据写为 gzip（压缩）文件，将结构化事实写为 Parquet + Zstd（列式压缩格式），并逐文件登记摘要
- [x] 5.2 生成只引用 Parquet 文件的 DuckDB（嵌入式分析数据库）视图和按对象查询入口，不保存重复数据库副本
- [x] 5.3 实现按策略、运行、数据集、字段和时间范围物化 CSV（表格文件），并记录来源摘要与过滤条件
- [x] 5.4 为压缩原始数据、JSONL 日志和 Parquet 配置 Git LFS（大文件存储），验证远端支持与可恢复性，并记录首个真实归档的压缩比和容量

## 6. 模拟交易增量同步和计划任务

- [x] 6.1 实现活动模拟交易发现、独立游标、代码/快照/数据/日志增量同步和次日失败补齐
- [x] 6.2 实现关闭状态检测、一次最终同步、归因结束校验及停止跟踪
- [x] 6.3 使用 `schtasks.exe`（Windows 任务计划命令）实现北京时间 04:00 安装/查看/卸载入口和每 30 分钟最多 3 次重试，不增加常驻进程
- [x] 6.4 验证登录过期、网络失败和重试耗尽时保留最后完整版本并输出可操作状态
- [x] 6.5 验证模拟交易多次修改/替换代码时逐版本取回真实源码，并保存历史顺序/时间、`sourceBacktestId` 到代码 SHA256/文件的完整映射
- [x] 6.6 修复归因所有权：模拟交易只读取启动生命周期单一路径，回测以最终资产关联防止 Token 复用误认，并清理已误归档的历史回测源文件

## 7. 仓库 Skill 与使用说明

- [x] 7.1 创建 `.agents/skills/joinquant-archive-sync/` 单一真实 Skill（技能），自包含 `SKILL.md`、同步 CLI、依赖和参考资料，编排候选列表、指定回测、活动模拟交易、按需日志、查询和 CSV 导出
- [x] 7.2 创建 `.claude/skills/joinquant-archive-sync` 相对 SymbolicLink（符号链接），验证 Codex 与 Claude 发现同一 Skill 且 `SKILL.md` 和脚本 SHA256 一致
- [x] 7.3 编写简短使用说明，覆盖首次认证、明确目标格式、04:00 计划任务、重新登录、异常状态和禁止提交凭证

## 8. 发布形态端到端验证

- [x] 8.1 为目标校验、清单门禁、异常状态、容错日志解析、增量游标和原子提交各保留最小可运行回归测试
- [x] 8.2 从 Codex 和 Claude 的仓库 Skill 发布入口分别调用同一 `self-test`，用进程内生成的小型证据完整执行临时归档、重复同步、查询和 CSV 输出；不得访问网络或历史数据
- [x] 8.3 用内存证据覆盖失败和取消运行、不合法日志、普通日志 1000 条边界、归因日志完整/缺页/断序/无终止/无写入器及不支持接口版本，验证没有对象被误报为全量完整
- [x] 8.4 创建临时计划任务并通过 `schtasks /Run` 调用同一 `self-test`，验证入口和退出状态后清理临时任务；正式运行仍只发生在聚宽云端
- [x] 8.5 运行仓库 full（完整）验证，逐项复核能力规格并记录已验证、外部受限和未验证项目
