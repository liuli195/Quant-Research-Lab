## 1. 自动化运行基础

- [x] 1.1 先从 `scheduled-sync-pr` 发布入口编写运行锁、原子 `last-run.json` 和用户当前工作区不受影响的失败测试，再实现最小编排模块
- [ ] 1.2 为 JoinQuant（聚宽）认证、GitHub 凭证、Codex 优先且 Claude 回退的 PR Flow runtime（运行时）官方插件发现和远端 `main` 基线编写前置失败测试，再把检查接入任何归档写入之前
- [x] 1.3 为固定专用 worktree（工作树）的首次创建、干净复用、身份异常和预先存在脏状态编写测试，再实现仅接受已知安全状态的准备逻辑

## 2. 同步与提交门禁

- [x] 2.1 从 `scheduled-sync-pr` 调用现有 `sync-active-simulations`，为 `unchanged` 结果和已经取得身份的 `failed` 结果补充路径归属所需的策略/模拟交易 ID，并测试全部未变化和任一对象失败的批次语义，确保失败时不进入 Git/PR 阶段
- [ ] 2.2 复用现有 `verify` 校验变化对象，并实现只允许策略索引、对应策略清单/默认代码和对应模拟交易归档的精确 Git 路径门禁
- [ ] 2.3 测试部分同步失败和范围外变化，记录结构化失败后只回滚本次运行产生的已识别归档路径，不清理运行前脏状态或范围外文件
- [ ] 2.4 测试无变化批次以 `noop` 成功退出；只有有效变化才创建 `codex/joinquant-archive-auto`、精确暂存并生成简体中文提交

## 3. PR Flow 闭环

- [ ] 3.1 测试并实现干净固定自动化分支优先恢复，不查询 PR 数量，不启动新同步批次或增加提交
- [ ] 3.2 默认重跑现有 PR Flow `complete` 完成推送、PR 创建或更新、检查、review gate（审查门禁）和合并；仅在已有结构化状态指向合并后清理时调用现有 `cleanup`，并把 stop state（停止状态）提升到最后运行状态
- [ ] 3.3 验证运行期 PR 不添加 `Fixes #11`，且任何 PR Flow 失败都不直推 `main`、不强制推送、不调用替代合并路径

## 4. 计划任务发布与兼容迁移

- [ ] 4.1 只把 `schedule-install` 的动作参数改为调用 `scheduled-sync-pr`，沿用现有任务 XML（配置文本）、项目 `.venv`、CLI 路径、工作目录、北京时间 04:00、三次重试和单实例策略
- [ ] 4.2 更新任务所有权识别测试，使新任务严格匹配且旧的直接同步任务仍可被安全识别和卸载，但不在本变更中替换生产任务
- [ ] 4.3 更新仓库 Skill（技能）和操作文档，说明无变化、失败状态、恢复命令、官方插件发现失败处理及生产任务迁移必须另行授权

## 5. 端到端验证

- [ ] 5.1 从 `scheduled-sync-pr` 发布命令使用临时 Git 远端和可控外部边界完成无变化、成功合并、同步失败和检查失败四条端到端回归
- [ ] 5.2 用单元测试验证生产任务 XML 的 `.venv`、`scheduled-sync-pr`、工作目录和参数，并通过 Skill 已有的临时 Windows `self-test` 任务验证 Task Scheduler 实际执行和返回状态，验收后只删除该临时任务
- [ ] 5.3 把新编排测试追加到现有 `verify.scheduler-unit`，运行 JoinQuant 同步目标测试和仓库 Full Verify（完整验证），逐项复核问题 #11 验收标准并记录无法在本地验证的真实远端部分
