## 1. 锁定项目契约

- [ ] 1.1 确认新海龟策略的本地 `strategy_id`（策略标识）、聚宽名称和项目目录，并验证不修改 `strategy-001`、`strategy-002`
- [ ] 1.2 用聚宽官方接口验证并锁定行情字段、日期范围、复权口径、快照身份和导出方式，形成可校验的项目快照契约
- [ ] 1.3 确认退出、风险减仓、建仓、加仓的同日优先级，以及现金/风险预算和整手余数分配规则，并先写成可执行验收夹具

## 2. 以 TDD（测试驱动开发）建立通用 Skill 与编排入口

- [ ] 2.1 先添加失败的 Skill（技能）布局和公开入口测试，覆盖名称、触发描述、`agents/openai.yaml`、单一 CLI（命令行接口）和 `.claude/skills/` 兼容链接
- [ ] 2.2 使用项目 `.venv`（虚拟环境）调用 `init_skill.py` 初始化 `.agents/skills/run-local-quant-research/`，补齐最小编排说明和兼容链接，使 2.1 测试及 `quick_validate.py` 通过
- [ ] 2.3 先添加失败的 JSON（结构化清单）配置测试，覆盖参数数组、仓库内路径、必需门禁和 `complete`、`evidence_insufficient`、`failed` 状态
- [ ] 2.4 实现配置校验与固定阶段编排，使 2.3 测试通过，并证明 Skill 只调用通用脚本和项目入口、不解释策略字段
- [ ] 2.5 先添加失败的运行身份、不可变清单、重复运行和篡改检测测试，再实现证据索引与幂等复用，使全部测试通过
- [ ] 2.6 添加环境与安全边界测试并实现对应门禁，验证只使用项目 `.venv`、不回退系统 Python、不静默安装依赖、不读取或打印凭证

## 3. 实现快照与 Data Bridge（数据桥）

- [ ] 3.1 先添加失败的快照清单测试，覆盖来源、快照标识、截止日、复权、字段、证券、导出代码和文件 SHA256（文件摘要），再实现通用校验脚本
- [ ] 3.2 先添加失败的 CSV（逗号分隔文件）与 DuckDB（嵌入式分析数据库）同源测试，覆盖类型、空值、排序、行差异和身份漂移，再实现规范化内容摘要与拒绝门禁
- [ ] 3.3 在海龟项目目录实现聚宽研究环境导出入口和 11 只 ETF（交易型开放式指数基金）快照清单生成，不把项目代码或数据复制进 Skill 目录
- [ ] 3.4 使用明确的聚宽研究运行取得并归档权威原始快照，逐项验证 11 只 ETF、必需字段、日期范围、复权口径和文件摘要
- [ ] 3.5 从已验证原始快照生成同源 CSV 与 DuckDB，运行通用校验并记录共同内容摘要和可追溯证据

## 4. 以 TDD 实现海龟策略与完整本地主流程

- [ ] 4.1 先添加失败的入场、TR（真实波幅）、20 日 N 值、0.5% 初始风险单位和次日执行测试，再实现对应纯计算模块
- [ ] 4.2 先添加失败的固定 0.5N 加仓、每日一次、预算裁剪和只上移 2N 共同止损测试，再实现对应状态逻辑
- [ ] 4.3 先添加失败的 20 日退出、保护性止损、整手、流动性、资金、单 ETF、资产组和组合计划风险测试，再实现全部硬门禁
- [ ] 4.4 先添加失败的 60 日协方差、10% 目标波动率缩减、冷启动和数据故障安全测试，再实现组合风险状态逻辑
- [ ] 4.5 先添加失败的完整 E2E（端到端）场景，覆盖收盘信号、次日订单、成交回填、批次/持仓/现金/风险状态和审计输出，再实现项目适配入口使场景通过
- [ ] 4.6 添加多类同日订单、预算争用、跳空、涨跌停、不可成交、退出和风险减仓场景，验证已确认优先级得到唯一且可追溯结果

## 5. 完成本地粗筛与研究报告

- [ ] 5.1 实现配置驱动的项目研究入口，接入确定性事件与风险计算，并对 Vibe-Trading（AI 研究助理）已知前视偏差组合优化器设置显式跳过门禁
- [ ] 5.2 使用已验证快照运行方向性粗筛与确定性复算，生成关联快照、代码、配置和输出摘要的研究证据
- [ ] 5.3 生成可复算研究报告，明确所有收益、交易和风险统计均为探索性非正式结果，且未据结果修改已确认基线参数

## 6. 验证 Skill 结构、流程和通用性

- [ ] 6.1 运行 `quick_validate.py`、Skill 布局、兼容链接和通用单元测试，修复全部失败并保留验证结果
- [ ] 6.2 从 Skill 文档公开的用户入口运行海龟完整离线 E2E，验证配置、快照、数据桥、项目入口、审计和证据索引完整贯通
- [ ] 6.3 在不加载海龟目录、参数、资产或代码的隔离环境运行通用测试，证明通用能力无海龟反向依赖
- [ ] 6.4 使用非海龟最小配置和原始夹具派发独立前向验证，复核代理能仅依据 Skill 完成研究流程并生成可验证证据

## 7. 接入 Build and Verify（构建与验证）并完成回归

- [ ] 7.1 更新 `.build-and-verify/config.json`，把 Skill、通用脚本、海龟项目、数据桥和相关测试映射到可受影响路径与验证命令，并验证配置可解释
- [ ] 7.2 运行覆盖用户入口完整业务流程的端到端回归和仓库完整验证，逐项复核两份 capability（能力）规格并记录已验证与无法验证部分
