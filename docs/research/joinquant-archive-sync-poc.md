# 聚宽归档同步真实 PoC

## 结论

状态：`PASS`

Browser（浏览器）+ Research（研究环境）双来源方案真实可行，并同时验证了两类目标：

1. 有归因写入器：归因 JSONL（逐行数据）完整闭合。
2. 无归因写入器：能用完整代码和 Research 证据判定 `missing_at_source`，不会伪造归因日志。

完整代码、免费普通日志、页面结果、官方收益概述、`get_backtest` 结构化数据和归因日志都已真实下载并复算 SHA256（摘要）。自动官方下载和人工补录两条路径都已验证。

## 主 PoC：存在归因写入器

- 策略：`etf_factor_rotation`
- 明确目标：详情页 `backtestId=121b439805c89d76b93c5ce520310c2d`
- 区间：2021-01-01 至 2026-04-30
- 页面状态：回测完成，页面显示实际耗时 01 分 47 秒
- 证据采集时间：2026-07-11 02:59:55（北京时间）

### 本地证据

证据只保存在忽略目录 `.local/joinquant-sync/poc`，不提交账号、Cookie（浏览器凭证）或访问 Token（令牌）。

| 文件 | 字节 | 行数 | SHA256 |
|---|---:|---:|---|
| `official/summary.csv` | 66,969 | 1,290 | `88cf692dfd06112331667fcf0938bfac8182693daf8698f501a1dea990423ec0` |
| `raw/default_code.py` | 62,965 | 1,456 | `79b2bdb2c82de35a722741b34292df7ed012f8348a19f1b0acfb209020322857` |
| `raw/browser-evidence.json` | 347,089 | 1 | `776de8457f094239e670587548452820780066b0fc6a8dac7f42194bcc086e22` |
| `raw/research-get-backtest.json` | 1,293,829 | 1 | `45bcb31fa83aebfc08301b02ad9139fcd827a211d50f0c8ed258195650e9e788` |
| `raw/audit_log.jsonl` | 1,697,552 | 1,631 | `de9033bd10d5e80d4c820efa8830c93e5ad632cf1a97055d7161f133bfd9cba8` |

### 结构化数据完整性

页面结果实际分三页返回：`1000 + 289 + 0`，合计 1,289 条。最后一页 0 条是明确终止证据。官方 CSV 为 1,289 个数据行加 1 个表头；Research 的收益序列也是 1,289 条，三者一致。

Research 官方 `get_backtest` 文件还包含：

- 持仓 2,386
- 订单 378
- `record` 记录 0
- 风险字段 31
- 分期风险 10 组
- 资金快照 1,289
- 源错误 0

首次用较小回测测试时，页面结果只有 `804 + 0`，会掩盖分页错误。本目标证明不能把第一页 `count` 当总数；必须累计每页条数，直到得到明确 0 页。

### 普通日志

- 偏移量 0 至 900：每页 100 条，共 1,000 条。
- 偏移量 1,000：返回 0 条且 `max=true`。
- 判定：`complete`，不是 `capped_free`。虽然数量恰好为 1,000，但下一页探针证明源端已结束。

### 归因日志

完整代码中存在归因事件写入、`run_start`、`run_end`、文件写入、运行 Token（令牌）和序列字段。实时下载的归因文件校验结果：

- 1,631 行全部为合法 JSON
- 首事件 `run_start`
- 末事件 `run_end`
- 序号 1 至 1,631，无断号、无重复
- 全部记录属于同一个运行 Token（令牌）
- 文件字节数与 SHA256 已登记

判定：`complete`。这套校验足以阻断缺页、断序、跨运行混入和缺少终止事件的归因日志。

## 对照 PoC：源头没有归因写入器

- 策略：`ETF动态调仓`
- 明确目标：回测列表第 4 个历史回测
- 区间：2023-01-01 至 2026-04-30
- 完整代码：396 行，SHA256 `3c3816643ecac8548b8b88c94e9b0d9484e253ef9b54a053ff15a7b621d0dee4`

代码中不存在归因事件、起止事件、序列、Token（令牌）、JSONL、文件写入或 `record` 调用；Research `get_records()` 也返回 0 条。

判定：`missing_at_source`。普通日志不能替代归因日志。

## 下载和认证路径

- 自动路径：页面“收益概述”官方下载成功；Research 完整结构化文件和归因日志下载成功。
- 人工路径：同一官方 CSV 经 `verify --import-file` 进入证据包，66,969 字节和 SHA256 与自动下载一致。
- 认证：首次在仓库外专用浏览器目录登录，按 Playwright 官方 `storage_state`（认证状态）保存到该目录；关闭浏览器后，Skill 的 Python 命令成功恢复登录。
- 敏感边界：`storage-state.json` 含浏览器认证信息，只能留在用户本机仓库外目录，禁止提交、打印或复制到归档。
- 积分路径：交易、持仓和完整日志只验证价格预览，均未确认。Research 已免费取得持仓和订单，普通日志也已免费完整取得，因此两个目标都不需要积分下载。

## 可行性边界

聚宽官方 API 文档明确说明 Research 中的 `get_backtest` 可取得状态、参数、收益、持仓、订单、`record`、风险和分期风险，但不提供策略代码和普通日志。因此只用 Research 不完整；Browser 负责完整代码、普通日志和官方下载，Research 负责结构化结果与归因文件，两者以清单、条数、终止探针和 SHA256 交叉校验。

官方依据：

- 聚宽 API 文档：<https://cdn.joinquant.com/help/img/JoinQuantAPI.pdf>（“研究中获取回测信息”章节）
- Playwright 认证状态：<https://playwright.dev/python/docs/auth>
