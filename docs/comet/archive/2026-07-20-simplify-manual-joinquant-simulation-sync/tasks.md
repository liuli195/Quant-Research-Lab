## 1. 移除自动化编排

- [x] 1.1 删除 `scheduled-sync-pr` 与所有计划任务 CLI（命令行接口）分支、专用 worktree（工作树）编排、运行锁和自动 PR Flow（拉取请求流程）调用，保留 `sync-active-simulations` 手动同步行为。
- [x] 1.2 删除已无调用者的调度实现，并清除 Skill（技能）与操作文档中的每日 04:00、重试、工作树和自动发布说明。

## 2. 回归与验收

- [x] 2.1 将调度和自动发布测试替换为手动 `sync-active-simulations` 入口回归；覆盖成功、对象失败以及不创建 Git（版本管理）提交、PR、工作树或任务计划。
- [x] 2.2 运行手动入口的端到端回归、`self-test`（自检）和仓库快速验证；完整模式需另行用户确认。已确认移除的 CLI（命令行接口）返回参数错误。
- [x] 2.3 按 Ponytail Review（轻量化审查）合并重复的手动入口测试，复用既有自检验证检查项，并删除布局测试的全局并行约束和 delta spec（差异规格）中的重复场景。

## 3. 运行期迁移

- [x] 3.1 用户已在 PR（拉取请求）前单独授权卸载 `JoinQuantArchiveSync` 并移除专用工作树；已确认任务和工作树不存在，保留遗留分支 `codex/joinquant-archive-auto` 及提交 `c59ec24`。
